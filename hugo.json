{
  "baseURL": "https://gomatic.github.io/docs.go-flagged/",
  "title": "go-flagged Documentation",
  "publishDir": "_site",
  "enableRobotsTXT": true,
  "markup": {
    "goldmark": {
      "renderHooks": {
        "link": {
          "useEmbedded": "fallback"
        },
        "image": {
          "useEmbedded": "fallback"
        }
      }
    }
  },
  "params": {
    "description": "Reflection-driven flag binding for Go: Bind registers a struct\u2019s fields on a flag.FlagSet with derived names and typed defaults."
  }
}
